# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon files
Icon?

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created by macOS
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Memory stores are intentionally TRACKED in git so their history is kept
# across sessions (see .commandcode/project-brain.jsonl, task-journal.jsonl).
# Removed from ignore list on purpose — do not re-add.
